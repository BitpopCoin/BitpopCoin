To build RNGcoind (daemon), go to /src/ and do 
make -f Makefile.unix if on Lunix systems
make -f Makefile.osx if on OSX

To build QT, (only managed to build linux right now)
$qmake RNGcoin-qt.pro
$make

make sure you do those in order
